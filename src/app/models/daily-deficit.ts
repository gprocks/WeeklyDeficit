export interface DailyDeficit {
  DateTime: string;
  // These are dateTime and value
  CaloriesIn: string;
  CaloriesOut: string;
}

export interface FitbitDailyCalorie {
  dateTime: string;
  // These are dateTime and value
  value: string;
}
